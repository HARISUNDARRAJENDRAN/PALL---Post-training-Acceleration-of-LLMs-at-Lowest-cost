# DocSmile VLM training stack — pinned for CUDA 12.1 / L40S (Ada sm_89)
# install torch FIRST (see setup.sh), then this file.
transformers==4.46.3
accelerate==1.1.1
trl==0.12.1
peft==0.13.2
datasets==3.1.0
pillow==11.0.0
sentencepiece==0.2.0
protobuf==5.28.3
huggingface_hub==0.26.2
hf_transfer==0.1.8
bitsandbytes==0.44.1      # fallback / 8-bit optimizer (not required for bf16 LoRA)
deepspeed==0.15.4         # optional (single-GPU doesn't need it)
tensorboard==2.18.0
# flash-attn is installed separately in setup.sh (prebuilt wheel, --no-build-isolation)
